{
  "name": "oakwood-junk-removal",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "npm run build && node --test 'test/**/*.test.js'"
  },
  "devDependencies": {
    "node-html-parser": "^9.0.4"
  }
}
